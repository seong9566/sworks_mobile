import 'package:flutter/material.dart';

class SiteEditView extends StatelessWidget {
  const SiteEditView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
